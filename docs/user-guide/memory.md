---
title: xAgent 长期记忆：跨会话保留偏好与决策
description: 了解 xAgent 如何在用户明确要求时保存长期记忆，并在后续会话中召回偏好、决策、边界和已验证事实。
status: experimental
updated: 2026-07-30
---

# xAgent 长期记忆：跨会话保留偏好与决策

> 状态：实验性能力，接口可能变化。

## 适用对象

本文适合希望 xAgent 跨会话保留稳定偏好、已确认决策或长期背景的用户。

## 这是什么

长期记忆用于延续当前用户已经明确要求保留的信息。它是跨会话的背景上下文，不等同于会话历史、上下文压缩，也不会把所有聊天自动变成长期事实。

## 什么时候使用

当某些信息会持续影响后续任务时，可以明确让 xAgent 记住，例如：

- 稳定的输出语言、格式和协作偏好。
- 项目的长期背景。
- 已确认的关键决策。
- 不能越过的业务边界或协作规则。

## 基本用法

### 明确要求记住

在当前消息中直接说明要跨会话保留的事实：

```text
请记住：这个项目的周报以后默认使用中文，并包含风险和下一步。
```

xAgent 只有在当前用户消息明确要求“记住、保存或长期保留”时，才会登记长期记忆。登记成功表示后台写入任务已经排队，不代表所有对话内容都会被自动保存。

### 召回以前的信息

需要使用以前的偏好或决策时，可以直接说明主题：

```text
请查一下我之前关于这个项目周报格式的约定，再生成本周周报。
```

xAgent 会在相关记忆可能实质影响当前任务时查询当前用户的长期记忆。查询结果仍会结合来源、更新时间、置信度和当前事实判断，不能替代对实时外部信息的核验。

### 修改当前要求

如果当前要求与旧记忆冲突，直接给出新的明确要求：

```text
这次周报改用英文，只对本次任务生效。
```

当前用户输入、工具结果、权限和系统规则优先于旧记忆。临时调整不会自动改写长期记忆；需要长期修改时，应再次明确要求记住新的规则。

## 使用边界

| 信息 | 是否适合作为长期记忆 |
| --- | --- |
| 稳定偏好、已确认决策和长期边界 | 适合，并应明确要求记住 |
| 临时任务、当前进度和一次性文件路径 | 不适合 |
| 密码、Token、私钥等秘密 | 不应保存 |
| 助手自己的建议或未经确认的推断 | 不应保存 |
| 实时价格、新闻和外部系统当前状态 | 应重新查询来源 |
| 其他用户或无权访问的数据 | 不可读取或保存 |

## 隐私与准确性

- 长期记忆按当前用户边界读取，不会因为知道其他会话或用户的名称就自动获得访问权。
- 记忆只提供背景连续性，不证明某项操作已经执行，也不扩大当前 Session 的职责和权限。
- 低置信度、过期、冲突或已被替代的记忆不能作为已确认事实直接使用。
- 记忆中保留原始来源时，依赖该记忆的回答应继续保留可核对的来源链接。

## 相关文档

- [Agent 会话](/docs/user-guide/agent-session)
- [长任务](/docs/user-guide/long-task)
- [术语表](/docs/reference/glossary)
